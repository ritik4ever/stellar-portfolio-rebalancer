# Alertmanager

Critical alerts page the on-call engineer via PagerDuty (or Opsgenie).
Escalation: 15m support, 30m manager.

Critical alerts are grouped by subsystem and service to avoid duplicate pages for the same incident.
