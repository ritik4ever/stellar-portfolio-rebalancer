# Repository-level npm config — resolves CI ERESOLVE failures from peer
# dependency conflicts in transitive deps (e.g. storybook ecosystem).
# Mirrors what `npm install --legacy-peer-deps --no-audit` invokes.
legacy-peer-deps=true
engine-strict=false
strict-peer-dependencies=false
fund=false
audit-level=low
